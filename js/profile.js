// V1: single hard-coded profile. When adding profile #2, this becomes a small
// registry array plus a profile-picker screen — everything else in the app
// stays the same (see the plan's "Generalizing to other profiles" section).
export const ACTIVE_PROFILE = {
  id: "business-conversational",
  displayName: "Business English — Conversational",
  level: "conversational",
};
